1.脏读
  一个事务读取到了另一个事务还未提交的数据，如果另一个事务此时回滚了，则读到的数据是无效的
2.不可重复读
  在同一事务中，多次读取同一数据但返回结果不同
3.幻读
  一个读取了部分记录以后，另一个事务插入了一些记录

隔离级别
  DEFAULT                  默认的隔离级别（spring）
  READ_UNCOMMITED          允许读取改变但未提交的数据，可能导致1,2,3
  READ_COMMITED            允许在并发事务已经提交后读取，防止1，但可能2,3                           Oracle
  REPEATABLE_READ          对相同字段的多次读取时一致的，除非数据被事务本身改变。可防止1,2，但2可能发生  MySql
  SERIALIZABLE             完全服从ACID，最慢，完全锁定事务中的数据实现


